class FolderManager{

    dir : string;
    recursive : boolean;

    constructor(dir : string, recursive : boolean){

        this.dir = dir;
        this.recursive = recursive;

    }

}

this.exports = FolderManager;